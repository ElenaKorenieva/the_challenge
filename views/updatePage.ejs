<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />
  </head>
  <body class="d-flex flex-column">
    <nav
      class="container-fluid navbar bg-secondary border-bottom border-info"
      data-bs-theme="dark"
    >
      <h1 class="text-light ms-4 py-1 fs-2">Welcome To Facebook</h1>
    </nav>
    <div class="container-fluid px-5">
        <a href="/feed">Back</a>
      <h3 class="mt-2"><%= singlePost.name %></h3>
      <p class="py-2 fs-5"><%= singlePost.message %></p>
      <small class="text-muted">Posted on <%= singlePost.getDate %></small>
      <div class="d-flex flex-row gap-2">
        <form action="/feed/<%= singlePost._id %>/delete-post" method="post" class="d-inline">
          <button type="submit" class="btn btn-danger">Delete</button>
        </form>
        <form action="/feed/edit/<%= singlePost._id %>" class="d-inline">
            <button type="submit" class="btn btn-info" style="color: white">Edit</button>
          </form>
    </div>
  </body>
</html>
