<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />
  </head>
  <body class="d-flex flex-column">
    <nav
      class="container-fluid navbar bg-secondary border-bottom border-info"
      data-bs-theme="dark"
    >
      <h1 class="text-light ms-4 py-1 fs-2">Welcome To Facebook</h1>
    </nav>
    <div class="container-fluid px-5">
      <form action="/feed" method="post">
        <button type="submit" class="btn-primary">Add a post</button>
        <input
          type="text"
          name="name"
          id="name"
          placeholder="Write a name"
          maxlength="15"
          required
        />
        <textarea
          name="message"
          id="message"
          cols="30"
          rows="10"
          maxlength="40"
          placeholder="Write your post"
          required
        ></textarea>
      </form>

      <section>
        <div class="container my-4 py-2">
          <div class="row d-flex justify-content-center">
            <% if(posts.length >0) { %> <% posts.forEach(post => { %>
            <div class="card mb-3 py-3">
              <div class="card-body">
                <h3><%= post.name %></h3>
                <p class="py-2 fs-5"><%= post.message %></p>
                <small class="text-muted">Posted on <%= post.getDate %></small>
              </div>
              <a href="/feed/<%= post._id %>">See more</a>
            </div>
            <% }) %> <% } else { %>
            <p>Nothing to display...</p>
            <% } %>
          </div>
        </div>
      </section>
    </div>
  </body>
</html>
